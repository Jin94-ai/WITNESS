"""Neural behavior policies — PyTorch backend.

Skeleton only (Spike 6 Phase A). Concrete model / trainer / dataset land in
subsequent phases. Everything here is person-agnostic; agent-specific
trained artifacts live under ``content/<agent>/trained/``.
"""
